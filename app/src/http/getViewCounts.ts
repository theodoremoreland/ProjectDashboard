// Types
import { ViewCount } from '../types';

// TODO: Placeholder
const getViewCounts = async (): Promise<ViewCount[]> => {
    return new Promise<ViewCount[]>((resolve) => {
        const viewCounts: ViewCount[] = [];
        resolve(viewCounts);
    });
};

export default getViewCounts;
